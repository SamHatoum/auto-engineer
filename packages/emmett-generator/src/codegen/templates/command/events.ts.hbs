import type { Event } from '@event-driven-io/emmett';

{{#each events}}
    export type {{pascalCase type}} = Event<
    '{{type}}',
    {
    {{#each fields}}
        {{name}}: {{{tsType}}};
    {{/each}}
    }
    >;
{{/each}}