<% atoms.forEach(atom => { -%>
import { <%= atom %> } from "@mui/material";
<% }) -%>
<% molecules.forEach(mol => { -%>
import { <%= mol %> } from "@/components/molecules/<%= mol %>";
<% }) -%>

// <%= description %>
<% if (specs && specs.length) { -%>
// Specs:
<% specs.forEach(spec => { -%>
// - <%- spec %>
<% }) -%>
<% } -%>

export function <%= name %>() {
    return <div />;
}